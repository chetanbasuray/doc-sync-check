# Milestone 1.4.0 Delivery Notes

This branch delivers milestone 1.4.0 scope:

- #54 Website scaffold in `website/` with GitHub Pages workflow.
- #51 JavaScript + JSDoc parsing support in CLI extractor pipeline.
- #48 AST logic exported via `src/ast/` entrypoint to decouple usage from CLI.
- #47 Coverage export formats for Sonar (`json`) and Cobertura (`xml`).
- #46 Incremental scanning cache via `.doc-sync-cache.json`.
- #45 Slack/Discord webhook notifications on drift failures.
- #44 `--init` setup wizard for first-time users.
- #43 Cross-file friendliness by scanning TS/JS trees and retaining imported type names in signatures.
- #42 `--fix-docs` auto-normalization for inline signature formatting.
- #41 VSCode extension scaffold in `.vscode-extension/`.
- #40 Docusaurus/VuePress markdown pattern support defaults.
- #39 README signature section automation via `--update-readme` markers.
- #38 Optional JSDoc description synchronization checks (`--check-descriptions`).
