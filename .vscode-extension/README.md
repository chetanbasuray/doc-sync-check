# doc-sync-check VSCode Extension (Scaffold)

This scaffold reserves the extension package for inline drift warnings.

Planned behavior:
- run `doc-sync-check` on markdown and source changes
- surface out-of-sync signatures as diagnostics
- quick-fix to normalize signature blocks
